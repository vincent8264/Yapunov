# Models

Place hackathon ONNX models in this directory. Model binaries are git-ignored; document each model's expected input shape, normalization, output meaning, and source here when one is selected.
