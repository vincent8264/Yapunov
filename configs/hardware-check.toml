# Safe laptop example. Change the backend to "uno_q" only inside Arduino App Lab.
[hardware]
type = "mock"
verbose = false

[hardware_check]
led = true
led_hold_seconds = 0.25

# Add actuator checks only after verifying wiring, voltage, pins, and travel limits.
# They remain skipped unless the command includes --allow-actuators.
# [[hardware_check.actuators]]
# type = "pwm"
# channel = 0
# value = 0.1
#
# [[hardware_check.actuators]]
# type = "servo"
# channel = 0
# value = 90.0
# safe_value = 90.0
